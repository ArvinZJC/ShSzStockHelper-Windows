<!-- A resource dictionary for a calendar with the Material Dark theme. -->
<!-- ReSharper disable Xaml.MissingGridIndex -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:helpers="clr-namespace:ShSzStockHelper.Helpers"
                    xmlns:properties="clr-namespace:ShSzStockHelper.Properties"
                    xmlns:syncfusionPrimitives="clr-namespace:Syncfusion.Windows.Controls.Primitives;assembly=Syncfusion.Shared.WPF"
                    xmlns:syncfusionSharedControls="clr-namespace:Syncfusion.Windows.Controls;assembly=Syncfusion.Shared.WPF"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Syncfusion.Themes.MaterialDark.WPF;component/Common/Brushes.xaml" />
        <ResourceDictionary Source="/Syncfusion.Themes.MaterialDark.WPF;component/Common/Common.xaml" />
        <ResourceDictionary Source="/Syncfusion.Themes.MaterialDark.WPF;component/MSControl/GlyphButton.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <helpers:FontFamilyNameConverter x:Key="FontFamilyNameConverter" />

    <Style x:Key="WpfCalendarButtonStyle" TargetType="{x:Type CalendarButton}">
        <Setter Property="Background" Value="{StaticResource ContentBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ContentBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource ContentForeground}"/>
        <Setter Property="FontFamily" Value="{Binding DisplayFontFamilyName, Converter={StaticResource FontFamilyNameConverter}, Source={x:Static properties:Settings.Default}}"/>
        <Setter Property="FontSize" Value="{StaticResource MaterialDark.BodyTextStyle}"/>
        <Setter Property="FontWeight" Value="{StaticResource MaterialDark.FontWeightNormal}"/>
        <Setter Property="MinWidth" Value="45"/>
        <Setter Property="MinHeight" Value="47"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarButton}">
                    <Grid>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0:0:0.1"/>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver"/>
                                <VisualState x:Name="Pressed"/>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SelectionStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0"/>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Unselected"/>
                                <VisualState x:Name="Selected"/>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="ActiveStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0"/>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Active"/>
                                <VisualState x:Name="Inactive">
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="CalendarButtonFocusStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0"/>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="CalendarButtonFocused"/>
                                <VisualState x:Name="CalendarButtonUnfocused"/>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Rectangle x:Name="SelectedBackground"
                                   Fill="{StaticResource PrimaryBackground}"
                                   Opacity="0"
                                   RadiusY="1"
                                   RadiusX="1"/>
                        <Rectangle x:Name="Background"
                                   Fill="{TemplateBinding Background}"
                                   Stroke="{TemplateBinding BorderBrush}"
                                   Opacity="0"
                                   RadiusY="1"
                                   RadiusX="1"/>
                        <ContentPresenter x:Name="NormalText"
                                          TextElement.Foreground="{TemplateBinding Foreground}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Margin="1,0,1,1"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <ContentPresenter.Resources>
                                <Style BasedOn="{x:Null}" TargetType="{x:Type TextBlock}"/>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                        <Rectangle x:Name="CalendarButtonFocusVisual"
                                   IsHitTestVisible="false"
                                   RadiusY="1" RadiusX="1"
                                   Stroke="{StaticResource PrimaryBackground}"
                                   Visibility="Collapsed"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" TargetName="Background" Value="1"/>
                            <Setter Property="Fill" TargetName="Background" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="Stroke" TargetName="Background" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForeground}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Visibility" TargetName="CalendarButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryForeground}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" TargetName="Background" Value="1"/>
                            <Setter Property="Fill" TargetName="Background" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="Stroke" TargetName="Background" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForeground}"/>
                        </Trigger>
                        <Trigger Property="IsInactive" Value="True">
                            <Setter Property="Opacity" TargetName="Background" Value="1"/>
                            <Setter Property="Fill" TargetName="Background" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="Stroke" TargetName="Background" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource DisabledForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource WpfCalendarButtonStyle}" TargetType="{x:Type CalendarButton}"/>

    <Style x:Key="WpfCalendarDayButtonStyle" TargetType="{x:Type CalendarDayButton}">
        <Setter Property="MinWidth" Value="5"/>
        <Setter Property="MinHeight" Value="5"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarDayButton}">
                    <Grid>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0:0:0.1"/>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver"/>
                                <VisualState x:Name="Pressed"/>
                                <VisualState x:Name="Disabled"/>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SelectionStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0"/>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Unselected"/>
                                <VisualState x:Name="Selected"/>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="CalendarButtonFocusStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0"/>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="CalendarButtonFocused">
                                </VisualState>
                                <VisualState x:Name="CalendarButtonUnfocused">
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="ActiveStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0"/>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Active"/>
                                <VisualState x:Name="Inactive">
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="DayStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0"/>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="RegularDay"/>
                                <VisualState x:Name="Today">
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="BlackoutDayStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0"/>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="NormalDay"/>
                                <VisualState x:Name="BlackoutDay">
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Border x:Name="TodayBackground"
                                 Background="{StaticResource ContentBackground}"
                                 BorderBrush="{StaticResource PrimaryBackground}"
                                 BorderThickness="1"
                                 Opacity="0"
                                 CornerRadius="14"
                                 Width="24" Height="24">
                        </Border>
                        <Border x:Name="SelectedBackground"
                                 Background="{StaticResource PrimaryBackground}"
                                 Opacity="0"
                                 CornerRadius="14"
                                 Width="24" Height="24">
                        </Border>
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}"/>
                        <Border x:Name="HighlightBackground"
                                 Background="{StaticResource ContentBackgroundHover}"
                                 BorderBrush="{StaticResource ContentBackgroundHover}"
                                 BorderThickness="1"
                                 Opacity="0"
                                 CornerRadius="14"
                                 Width="24" Height="24">
                        </Border>
                        <ContentPresenter x:Name="NormalText"
                                          TextElement.Foreground="{StaticResource ContentForeground}"
                                          TextElement.FontSize="{StaticResource MaterialDark.BodyTextStyle}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Margin="5,1,5,1"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <ContentPresenter.Resources>
                                <Style BasedOn="{x:Null}" TargetType="{x:Type TextBlock}"/>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                        <Path x:Name="Blackout"
                              Data="M0,0 L16,0 16,2 0,2 z"
                              Fill="{StaticResource ContentBackground}"
                              HorizontalAlignment="Stretch"
                              Margin="4"
                              Height="1"
                              Opacity="0"
                              RenderTransformOrigin="0.5,0.5"
                              Stretch="Fill"
                              VerticalAlignment="Stretch"/>
                        <Border x:Name="DayButtonFocusVisual"
                                 IsHitTestVisible="false"
                                 Width="24" Height="24"
                                 BorderBrush="{StaticResource PrimaryBackground}"
                                 BorderThickness="1"
                                 CornerRadius="14"
                                 Visibility="Collapsed">
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryForeground}"/>
                        </Trigger>
                        <Trigger Property="IsToday" Value="True">
                            <Setter Property="Opacity" TargetName="TodayBackground" Value="1"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryColorForeground}"/>
                        </Trigger>
                        <Trigger Property="IsInactive" Value="True">
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                        </Trigger>
                        <Trigger Property="IsBlackedOut" Value="True">
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Stroke" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryForeground}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForeground}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource DisabledForeground}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Opacity" TargetName="TodayBackground" Value="0"/>
                            <Setter Property="Width" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Height" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Opacity" TargetName="TodayBackground" Value="0"/>
                            <Setter Property="Width" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Height" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="0"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource PrimaryBackgroundOpacity}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryColorForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsToday" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource DisabledForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsInactive" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsInactive" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsInactive" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource DisabledForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundAlt3}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundAlt3}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForeground}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource DisabledForeground}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="TodayBackground" Value="0"/>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Width" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Height" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundSelection}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="TodayBackground" Value="0"/>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Width" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Height" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundSelection}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsToday" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource DisabledForeground}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource DisabledForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsInactive" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsInactive" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsInactive" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource WpfCalendarDayButtonStyle}" TargetType="{x:Type CalendarDayButton}"/>

    <Style x:Key="WpfCalendarItemStyle" TargetType="{x:Type CalendarItem}">
        <Setter Property="Margin" Value="0,3,0,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarItem}">
                    <ControlTemplate.Resources>
                        <DataTemplate x:Key="{x:Static CalendarItem.DayTitleTemplateResourceKey}">
                            <TextBlock Foreground="{StaticResource ContentForeground}"
                                       FontWeight="{StaticResource MaterialDark.FontWeightMedium}"
                                       FontSize="{StaticResource MaterialDark.BodyTextStyle}"
                                       FontFamily="{Binding DisplayFontFamilyName, Converter={StaticResource FontFamilyNameConverter}, Source={x:Static properties:Settings.Default}}"
                                       HorizontalAlignment="Center"
                                       Margin="0,6,0,6"
                                       Text="{Binding}"
                                       VerticalAlignment="Center"/>
                        </DataTemplate>
                    </ControlTemplate.Resources>
                    <Grid x:Name="PART_Root">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation Duration="0" To="1" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="PART_DisabledVisual"/>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="1">
                            <Border BorderBrush="{StaticResource ContentBackground}" BorderThickness="2" CornerRadius="1">
                                <Grid>
                                    <Grid.Resources>
                                        <Style
                                            x:Key="MaterialPreviousArrowButtonStyle"
                                            BasedOn="{StaticResource WPFGlyphButtonStyle}"
                                            TargetType="{x:Type Button}">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <Path
                                                        x:Name="path"
                                                        Width="5"
                                                        Height="8"
                                                        Margin="0"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        Data="M4,0 L4,7 0,3.5000001 z"
                                                        Stretch="Fill"
                                                        Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                                                        StrokeThickness="1" />
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                        <Style
                                            x:Key="MaterialNextArrowButtonStyle"
                                            BasedOn="{StaticResource WPFGlyphButtonStyle}"
                                            TargetType="{x:Type Button}">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <Path
                                                        x:Name="path"
                                                        Width="5"
                                                        Height="8"
                                                        Margin="0"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        Data="M0,0 L4,3.5 0,7 z"
                                                        Stretch="Fill"
                                                        Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                                                        StrokeThickness="1" />
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                        <Style x:Key="MaterialHeaderButtonStyle" TargetType="{x:Type Button}">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type Button}">
                                                        <Grid Cursor="Hand">
                                                            <VisualStateManager.VisualStateGroups>
                                                                <VisualStateGroup x:Name="CommonStates">
                                                                    <VisualState x:Name="Normal"/>
                                                                    <VisualState x:Name="MouseOver"/>
                                                                    <VisualState x:Name="Disabled">
                                                                    </VisualState>
                                                                </VisualStateGroup>
                                                            </VisualStateManager.VisualStateGroups>
                                                            <ContentPresenter x:Name="buttonContent"
                                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                      Content="{TemplateBinding Content}"
                                                                      TextElement.Foreground="{StaticResource ContentForeground}"
                                                                      TextElement.FontFamily="{Binding DisplayFontFamilyName, Converter={StaticResource FontFamilyNameConverter}, Source={x:Static properties:Settings.Default}}"
                                                                      TextElement.FontSize="{StaticResource MaterialDark.TitleTextStyle}"
                                                                      TextElement.FontWeight="{StaticResource MaterialDark.FontWeightMedium}"
                                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                                                <ContentPresenter.Resources>
                                                                    <Style BasedOn="{x:Null}" TargetType="{x:Type TextBlock}"/>
                                                                </ContentPresenter.Resources>
                                                            </ContentPresenter>
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="true">
                                                                <Setter Property="TextElement.Foreground" TargetName="buttonContent" Value="{StaticResource ContentForeground}"/>
                                                            </Trigger>
                                                            <Trigger Property="IsEnabled" Value="false">
                                                                <Setter Property="TextElement.Foreground" TargetName="buttonContent" Value="{StaticResource DisabledForeground}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Grid.Resources>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="28"/>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="28"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Button x:Name="PART_PreviousButton"
                                            Grid.Column="0"
                                            Focusable="False"
                                            HorizontalAlignment="Left"
                                            Width="28"
                                            Height="25"
                                            Grid.Row="0"
                                            Style="{StaticResource MaterialPreviousArrowButtonStyle}"/>
                                    <Button x:Name="PART_HeaderButton"
                                            Grid.Column="1"
                                            FontWeight="Bold"
                                            Focusable="False"
                                            FontSize="10.5"
                                            Height="25"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            Grid.Row="0"
                                            Style="{StaticResource MaterialHeaderButtonStyle}"/>
                                    <Button x:Name="PART_NextButton"
                                            Grid.Column="2"
                                            Focusable="False"
                                            HorizontalAlignment="Right"
                                            Width="28"
                                            Height="25"
                                            Grid.Row="0"
                                            Style="{StaticResource MaterialNextArrowButtonStyle}"/>
                                    <Grid x:Name="PART_MonthView"
                                          Grid.ColumnSpan="3"
                                          HorizontalAlignment="Center"
                                          Margin="6"
                                          Grid.Row="1"
                                          Visibility="Visible">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                    </Grid>
                                    <Grid x:Name="PART_YearView"
                                          Grid.ColumnSpan="3"
                                          HorizontalAlignment="Center"
                                          Margin="6,16,6,16"
                                          Grid.Row="1"
                                          Visibility="Hidden">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Border>
                        <Rectangle x:Name="PART_DisabledVisual"
                                   Fill="{StaticResource ContentBackground}"
                                   Opacity="0"
                                   RadiusY="2" RadiusX="2"
                                   Stretch="Fill"
                                   Stroke="{StaticResource ContentBackground}"
                                   StrokeThickness="1"
                                   Visibility="Collapsed"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Visibility" TargetName="PART_DisabledVisual" Value="Visible"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding DisplayMode, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Calendar}}}" Value="Year">
                            <Setter Property="Visibility" TargetName="PART_MonthView" Value="Hidden"/>
                            <Setter Property="Visibility" TargetName="PART_YearView" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding DisplayMode, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Calendar}}}" Value="Decade">
                            <Setter Property="Visibility" TargetName="PART_MonthView" Value="Hidden"/>
                            <Setter Property="Visibility" TargetName="PART_YearView" Value="Visible"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource WpfCalendarItemStyle}" TargetType="{x:Type CalendarItem}"/>

    <Style x:Key="WpfCalendarStyle" TargetType="{x:Type Calendar}">
        <Setter Property="Foreground" Value="{StaticResource ContentForeground}"/>
        <Setter Property="Background" Value="{StaticResource ContentBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderAlt}"/>
        <Setter Property="FontFamily" Value="{Binding DisplayFontFamilyName, Converter={StaticResource FontFamilyNameConverter}, Source={x:Static properties:Settings.Default}}"/>
        <Setter Property="FontSize" Value="{StaticResource MaterialDark.BodyTextStyle}"/>
        <Setter Property="FontWeight" Value="{StaticResource MaterialDark.FontWeightNormal}"/>
        <Setter Property="CalendarItemStyle" Value="{StaticResource WpfCalendarItemStyle}"/>
        <Setter Property="CalendarDayButtonStyle" Value="{StaticResource WpfCalendarDayButtonStyle}"/>
        <Setter Property="CalendarButtonStyle" Value="{StaticResource WpfCalendarButtonStyle}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Calendar}">
                    <StackPanel x:Name="PART_Root" HorizontalAlignment="Center">
                        <CalendarItem x:Name="PART_CalendarItem"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      Background="{TemplateBinding Background}"
                                      Style="{TemplateBinding CalendarItemStyle}"/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style BasedOn="{StaticResource WpfCalendarStyle}" TargetType="{x:Type Calendar}"/>

    <!--  DateTimEdit Calendar Style  -->

    <Style x:Key="SyncfusionCalendarButtonStyle" TargetType="{x:Type syncfusionPrimitives:CalendarButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{StaticResource ContentBackground}" />
        <Setter Property="Foreground" Value="{StaticResource ContentForeground}" />
        <Setter Property="FontFamily" Value="{Binding DisplayFontFamilyName, Converter={StaticResource FontFamilyNameConverter}, Source={x:Static properties:Settings.Default}}" />
        <Setter Property="FontSize" Value="{StaticResource MaterialDark.BodyTextStyle}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="FontWeight" Value="{StaticResource MaterialDark.FontWeightNormal}" />
        <Setter Property="MinWidth" Value="45" />
        <Setter Property="MinHeight" Value="47" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type syncfusionPrimitives:CalendarButton}">
                    <Grid>
                        <Rectangle
                            x:Name="SelectedBackground"
                            Fill="{StaticResource PrimaryBackground}"
                            Opacity="0"
                            RadiusX="1"
                            RadiusY="1" />
                        <Rectangle
                            x:Name="Background"
                            Fill="{TemplateBinding Background}"
                            Opacity="0"
                            RadiusX="1"
                            RadiusY="1"
                            Stroke="{TemplateBinding BorderBrush}" />
                        <ContentPresenter
                            x:Name="NormalText"
                            Margin="1,0,1,1"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            TextElement.Foreground="{TemplateBinding Foreground}">
                            <ContentPresenter.Resources>
                                <Style BasedOn="{x:Null}" TargetType="{x:Type TextBlock}" />
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                        <Rectangle
                            x:Name="CalendarButtonFocusVisual"
                            IsHitTestVisible="false"
                            RadiusX="1"
                            RadiusY="1"
                            Stroke="{StaticResource PrimaryBackground}"
                            Visibility="Collapsed" />
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0:0:0.1" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver" />
                                <VisualState x:Name="Pressed" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SelectionStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Unselected" />
                                <VisualState x:Name="Selected" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="ActiveStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Active" />
                                <VisualState x:Name="Inactive" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="CalendarButtonFocusStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="CalendarButtonFocused" />
                                <VisualState x:Name="CalendarButtonUnfocused" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Background" Property="Opacity" Value="1" />
                            <Setter TargetName="Background" Property="Fill" Value="{StaticResource ContentBackgroundHover}" />
                            <Setter TargetName="Background" Property="Stroke" Value="{StaticResource ContentBackgroundHover}" />
                            <Setter TargetName="NormalText" Property="TextElement.Foreground" Value="{StaticResource ContentForeground}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="CalendarButtonFocusVisual" Property="Visibility" Value="Visible" />
                            <Setter TargetName="SelectedBackground" Property="Opacity" Value="1" />
                            <Setter TargetName="NormalText" Property="TextElement.Foreground" Value="{StaticResource PrimaryForeground}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Background" Property="Opacity" Value="1" />
                            <Setter TargetName="Background" Property="Fill" Value="{StaticResource ContentBackgroundHover}" />
                            <Setter TargetName="Background" Property="Stroke" Value="{StaticResource ContentBackgroundHover}" />
                            <Setter TargetName="NormalText" Property="TextElement.Foreground" Value="{StaticResource ContentForeground}" />
                        </Trigger>
                        <Trigger Property="IsInactive" Value="True">
                            <Setter TargetName="Background" Property="Opacity" Value="1" />
                            <Setter TargetName="Background" Property="Fill" Value="Transparent" />
                            <Setter TargetName="Background" Property="Stroke" Value="Transparent" />
                            <Setter TargetName="NormalText" Property="TextElement.Foreground" Value="{StaticResource DisabledForeground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource SyncfusionCalendarButtonStyle}" TargetType="{x:Type syncfusionPrimitives:CalendarButton}" />

    <Style x:Key="SyncfusionCalendarDayButtonStyle" TargetType="{x:Type syncfusionPrimitives:CalendarDayButton}">
        <Setter Property="MinWidth" Value="5" />
        <Setter Property="MinHeight" Value="5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type syncfusionPrimitives:CalendarDayButton}">
                    <Grid>
                        <Border x:Name="TodayBackground"
                                 Background="{StaticResource ContentBackground}"
                                 BorderBrush="{StaticResource PrimaryBackground}"
                                 BorderThickness="1"
                                 Opacity="0"
                                 CornerRadius="14"
                                 Width="24" Height="24">
                        </Border>
                        <Border x:Name="SelectedBackground"
                                 Background="{StaticResource PrimaryBackground}"
                                 Opacity="0"
                                 CornerRadius="14"
                                 Width="24" Height="24">
                        </Border>
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}"/>
                        <Border x:Name="HighlightBackground"
                                 Background="{StaticResource ContentBackgroundHover}"
                                 BorderBrush="{StaticResource ContentBackgroundHover}"
                                 BorderThickness="1"
                                 Opacity="0"
                                 CornerRadius="14"
                                 Width="24" Height="24">
                        </Border>
                        <ContentPresenter x:Name="NormalText"
                                          TextElement.Foreground="{StaticResource ContentForeground}"
                                          TextElement.FontSize="{StaticResource MaterialDark.BodyTextStyle}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Margin="5,1,5,1"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <ContentPresenter.Resources>
                                <Style BasedOn="{x:Null}" TargetType="{x:Type TextBlock}"/>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                        <Path x:Name="Blackout"
                              Data="M0,0 L16,0 16,2 0,2 z"
                              Fill="{StaticResource ContentBackground}"
                              HorizontalAlignment="Stretch"
                              Margin="4"
                              Height="1"
                              Opacity="0"
                              RenderTransformOrigin="0.5,0.5"
                              Stretch="Fill"
                              VerticalAlignment="Stretch"/>
                        <Border x:Name="DayButtonFocusVisual"
                                 IsHitTestVisible="false"
                                 Width="24" Height="24"
                                 BorderBrush="{StaticResource PrimaryBackground}"
                                 BorderThickness="1"
                                 CornerRadius="14"
                                 Visibility="Collapsed">
                        </Border>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0:0:0.1" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver" />
                                <VisualState x:Name="Pressed" />
                                <VisualState x:Name="Disabled" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SelectionStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Unselected" />
                                <VisualState x:Name="Selected" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="CalendarButtonFocusStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="CalendarButtonFocused" />
                                <VisualState x:Name="CalendarButtonUnfocused" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="ActiveStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Active" />
                                <VisualState x:Name="Inactive" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="DayStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="RegularDay" />
                                <VisualState x:Name="Today" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="BlackoutDayStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="NormalDay" />
                                <VisualState x:Name="BlackoutDay" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryForeground}"/>
                        </Trigger>
                        <Trigger Property="IsToday" Value="True">
                            <Setter Property="Opacity" TargetName="TodayBackground" Value="1"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryColorForeground}"/>
                        </Trigger>
                        <Trigger Property="IsInactive" Value="True">
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="Transparent"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="Transparent"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                        </Trigger>
                        <Trigger Property="IsBlackedOut" Value="True">
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Stroke" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryForeground}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForeground}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource DisabledForeground}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Opacity" TargetName="TodayBackground" Value="0"/>
                            <Setter Property="Width" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Height" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Opacity" TargetName="TodayBackground" Value="0"/>
                            <Setter Property="Width" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Height" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="0"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource PrimaryBackgroundOpacity}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource PrimaryColorForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsToday" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource DisabledForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsInactive" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsInactive" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsInactive" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource DisabledForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundAlt3}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundAlt3}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForeground}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource DisabledForeground}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="TodayBackground" Value="0"/>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Width" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Height" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundSelection}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource IconDisabledColor}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="TodayBackground" Value="0"/>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Width" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Height" TargetName="SelectedBackground" Value="18"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundSelection}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsToday" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource PrimaryBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsToday" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource DisabledForeground}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource DisabledForeground}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsInactive" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="SelectedBackground" Value="1"/>
                            <Setter Property="Visibility" TargetName="DayButtonFocusVisual" Value="Visible"/>
                            <Setter Property="Background" TargetName="SelectedBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="DayButtonFocusVisual" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsInactive" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackgroundHover}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsInactive" Value="True"/>
                                <Condition Property="IsBlackedOut" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" TargetName="Blackout" Value="1"/>
                            <Setter Property="Opacity" TargetName="HighlightBackground" Value="1"/>
                            <Setter Property="Background" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="BorderBrush" TargetName="HighlightBackground" Value="{StaticResource ContentBackground}"/>
                            <Setter Property="TextElement.Foreground" TargetName="NormalText" Value="{StaticResource ContentForegroundAlt}"/>
                            <Setter Property="Fill" TargetName="Blackout" Value="{StaticResource ContentForegroundAlt}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource SyncfusionCalendarDayButtonStyle}" TargetType="{x:Type syncfusionPrimitives:CalendarDayButton}" />

    <Style x:Key="SyncfusionCalendarItemStyle" TargetType="{x:Type syncfusionPrimitives:CalendarItem}">
        <Setter Property="Margin" Value="0,3,0,3" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type syncfusionPrimitives:CalendarItem}">
                    <ControlTemplate.Resources>
                        <DataTemplate x:Key="DayTitleTemplate">
                            <TextBlock
                                Margin="0,6,0,6"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontFamily="{Binding DisplayFontFamilyName, Converter={StaticResource FontFamilyNameConverter}, Source={x:Static properties:Settings.Default}}"
                                FontSize="{StaticResource MaterialDark.BodyTextStyle}"
                                FontWeight="{StaticResource MaterialDark.FontWeightMedium}"
                                Foreground="{StaticResource ContentForeground}"
                                Text="{Binding}" />
                        </DataTemplate>
                    </ControlTemplate.Resources>
                    <Grid x:Name="PART_Root">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="1">
                            <Border
                                BorderThickness="2"
                                CornerRadius="1">
                                <Grid>
                                    <Grid.Resources>
                                        <Style
                                            x:Key="MaterialPreviousArrowButtonStyle"
                                            BasedOn="{StaticResource WPFGlyphButtonStyle}"
                                            TargetType="{x:Type Button}">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <Path
                                                        x:Name="path"
                                                        Width="5"
                                                        Height="8"
                                                        Margin="0"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        Data="M4,0 L4,7 0,3.5000001 z"
                                                        Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                                                        Stretch="Fill"
                                                        StrokeThickness="1" />
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                        <Style
                                            x:Key="MaterialNextArrowButtonStyle"
                                            BasedOn="{StaticResource WPFGlyphButtonStyle}"
                                            TargetType="{x:Type Button}">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <Path
                                                        x:Name="path"
                                                        Width="5"
                                                        Height="8"
                                                        Margin="0"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        Data="M0,0 L4,3.5 0,7 z"
                                                        Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                                                        Stretch="Fill"
                                                        StrokeThickness="1" />
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                        <Style x:Key="MaterialHeaderButtonStyle" TargetType="{x:Type Button}">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type Button}">
                                                        <Grid Cursor="Hand">
                                                            <ContentPresenter
                                                                x:Name="buttonContent"
                                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                Content="{TemplateBinding Content}"
                                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                TextElement.FontFamily="{Binding DisplayFontFamilyName, Converter={StaticResource FontFamilyNameConverter}, Source={x:Static properties:Settings.Default}}"
                                                                TextElement.FontSize="{StaticResource MaterialDark.TitleTextStyle}"
                                                                TextElement.FontWeight="{StaticResource MaterialDark.FontWeightMedium}"
                                                                TextElement.Foreground="{StaticResource ContentForeground}">
                                                                <ContentPresenter.Resources>
                                                                    <Style BasedOn="{x:Null}" TargetType="{x:Type TextBlock}" />
                                                                </ContentPresenter.Resources>
                                                            </ContentPresenter>
                                                            <VisualStateManager.VisualStateGroups>
                                                                <VisualStateGroup x:Name="CommonStates">
                                                                    <VisualState x:Name="Normal" />
                                                                    <VisualState x:Name="MouseOver" />
                                                                    <VisualState x:Name="Disabled" />
                                                                </VisualStateGroup>
                                                            </VisualStateManager.VisualStateGroups>
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="true">
                                                                <Setter TargetName="buttonContent" Property="TextElement.Foreground" Value="{StaticResource ContentForeground}" />
                                                            </Trigger>
                                                            <Trigger Property="IsEnabled" Value="false">
                                                                <Setter TargetName="buttonContent" Property="TextElement.Foreground" Value="{StaticResource DisabledForeground}" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Grid.Resources>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="28" />
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="28" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <Button
                                        x:Name="PART_PreviousButton"
                                        Grid.Row="0"
                                        Grid.Column="0"
                                        Width="28"
                                        Height="25"
                                        HorizontalAlignment="Left"
                                        Focusable="False"
                                        Style="{StaticResource MaterialPreviousArrowButtonStyle}" />
                                    <Button
                                        x:Name="PART_HeaderButton"
                                        Grid.Row="0"
                                        Grid.Column="1"
                                        Height="25"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Focusable="False"
                                        FontSize="10.5"
                                        FontWeight="Bold"
                                        Style="{StaticResource MaterialHeaderButtonStyle}" />
                                    <Button
                                        x:Name="PART_NextButton"
                                        Grid.Row="0"
                                        Grid.Column="2"
                                        Width="28"
                                        Height="25"
                                        HorizontalAlignment="Right"
                                        Focusable="False"
                                        Style="{StaticResource MaterialNextArrowButtonStyle}" />
                                    <Grid
                                        x:Name="PART_MonthView"
                                        Grid.Row="1"
                                        Grid.ColumnSpan="3"
                                        Margin="6"
                                        HorizontalAlignment="Center"
                                        Visibility="Visible">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                    </Grid>
                                    <Grid
                                        x:Name="PART_YearView"
                                        Grid.Row="1"
                                        Grid.ColumnSpan="3"
                                        Margin="6,16,6,16"
                                        HorizontalAlignment="Center"
                                        Visibility="Hidden">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Border>
                        <Rectangle
                            x:Name="PART_DisabledVisual"
                            Fill="{StaticResource ContentBackground}"
                            Opacity="0"
                            RadiusX="2"
                            RadiusY="2"
                            Stretch="Fill"
                            Stroke="{StaticResource ContentBackground}"
                            StrokeThickness="1"
                            Visibility="Collapsed" />
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="PART_DisabledVisual"
                                            Storyboard.TargetProperty="Opacity"
                                            To="1"
                                            Duration="0" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PART_DisabledVisual" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <DataTrigger Binding="{Binding DisplayMode, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type syncfusionSharedControls:Calendar}}}" Value="Year">
                            <Setter TargetName="PART_MonthView" Property="Visibility" Value="Hidden" />
                            <Setter TargetName="PART_YearView" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding DisplayMode, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type syncfusionSharedControls:Calendar}}}" Value="Decade">
                            <Setter TargetName="PART_MonthView" Property="Visibility" Value="Hidden" />
                            <Setter TargetName="PART_YearView" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource SyncfusionCalendarItemStyle}" TargetType="{x:Type syncfusionPrimitives:CalendarItem}" />

    <Style x:Key="SyncfusionCalendarStyle" TargetType="{x:Type syncfusionSharedControls:Calendar}">
        <Setter Property="Foreground" Value="{StaticResource ContentForeground}" />
        <Setter Property="Background" Value="{StaticResource ContentBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderAlt}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="FontFamily" Value="{Binding DisplayFontFamilyName, Converter={StaticResource FontFamilyNameConverter}, Source={x:Static properties:Settings.Default}}" />
        <Setter Property="FontSize" Value="{StaticResource MaterialDark.BodyTextStyle}" />
        <Setter Property="FontWeight" Value="{StaticResource MaterialDark.FontWeightNormal}" />
        <Setter Property="CalendarItemStyle" Value="{StaticResource SyncfusionCalendarItemStyle}" />
        <Setter Property="CalendarDayButtonStyle" Value="{StaticResource SyncfusionCalendarDayButtonStyle}" />
        <Setter Property="CalendarButtonStyle" Value="{StaticResource SyncfusionCalendarButtonStyle}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type syncfusionSharedControls:Calendar}">
                    <StackPanel x:Name="PART_Root" HorizontalAlignment="Center">
                        <syncfusionPrimitives:CalendarItem
                            x:Name="PART_CalendarItem"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Style="{TemplateBinding CalendarItemStyle}" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource SyncfusionCalendarStyle}" TargetType="{x:Type syncfusionSharedControls:Calendar}" />

    <!--  DateTimeEdit Calendar Style  -->

</ResourceDictionary>